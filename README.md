# Drydock

_(New project - skeleton scaffolded from Valiquo's top-level structure. Replace this with a real description as the project takes shape.)_

## Structure

- `src/` - Express backend
- `web/` - Next.js (App Router) frontend, own `package.json`
- `docs/` - architecture, handoff, plan, demo-script, deploy notes
- `scripts/` - test/ops scripts

## Running the backend

```sh
npm install
npm start
```

Serves on `http://localhost:3000` by default (see `.env`).
